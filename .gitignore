node_modules/
.next/
out/
dist/
.env
.env.local
.DS_Store
*.log
.cache/
.venv/
__pycache__/
*.egg-info/

# 取得データは再現可能なのでコミットしない(数百MB規模になる)。
# ただし data/viz/ だけは例外: ビューアが読む決定的な成果物(約15MB)で、
# Vercel の静的配信にそのまま使うためコミットする。
data/*
!data/viz/

